import React from 'react';
import ReactDOM from 'react-dom';
import IconMarketing from './IconMarketing.js';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<IconMarketing />, div);
});
